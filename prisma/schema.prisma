generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") //TODO: add envalid
}

model Game {
  id String @id @default(cuid())
  name String
}